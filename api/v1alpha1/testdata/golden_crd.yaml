---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: warmrunnerpolicies.autoscaling.warmrunners.io
spec:
  group: autoscaling.warmrunners.io
  names:
    categories:
    - warmrunners
    kind: WarmRunnerPolicy
    listKind: WarmRunnerPolicyList
    plural: warmrunnerpolicies
    shortNames:
    - wrp
    singular: warmrunnerpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.desiredFloor
      name: Desired
      type: integer
    - jsonPath: .status.appliedFloor
      name: Applied
      type: integer
    - jsonPath: .status.lastQueueDepth
      name: Queue
      type: integer
    - jsonPath: .status.predictedFloor
      name: Predicted
      type: integer
    - jsonPath: .status.activityFloor
      name: Active
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              activity:
                description: ActivityConfig configures the activity-driven warm-floor
                  signal (v0.3.0).
                properties:
                  botLoginDenylist:
                    description: |-
                      BotLoginDenylist is appended to the built-in denylist (dependabot[bot],
                      renovate[bot], github-actions[bot], mergify[bot], codecov[bot],
                      copilot-pull-request-reviewer[bot], self-hosted-renovate[bot]). Use
                      for PAT-driven machine users that lack the [bot] suffix (e.g.
                      "snyk-bot", "getsentry-bot", in-house "*-ci" / "*-deploy" accounts).
                      Entries match workflow_run.actor.login exactly (case-sensitive).
                    items:
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  enabled:
                    default: true
                    type: boolean
                  windowSeconds:
                    default: 900
                    description: |-
                      WindowSeconds is the rolling window over which recent non-bot
                      workflow_runs are counted. Defaults to 900 (15 minutes); see spec §3.4.
                    format: int32
                    maximum: 7200
                    minimum: 60
                    type: integer
                type: object
              floor:
                properties:
                  max:
                    format: int32
                    minimum: 0
                    type: integer
                  min:
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - max
                - min
                type: object
                x-kubernetes-validations:
                - message: min must be <= max
                  rule: self.min <= self.max
              github:
                properties:
                  auth:
                    properties:
                      secretRef:
                        description: SecretKeySelector selects a key of a Secret.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - secretRef
                    type: object
                  labels:
                    items:
                      maxLength: 64
                      type: string
                    maxItems: 64
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: atomic
                  owner:
                    maxLength: 253
                    type: string
                  repository:
                    description: |-
                      Repository is required: v1 supports repo-level polling only. An empty
                      value would build an invalid org-level URL (/repos/owner//actions/runs).
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - auth
                - labels
                - owner
                - repository
                type: object
              predictor:
                description: PredictorConfig configures the codebase-aware Predictor
                  (v0.2.0).
                properties:
                  enabled:
                    default: true
                    type: boolean
                  maxRunsPerPoll:
                    default: 50
                    format: int32
                    maximum: 500
                    minimum: 1
                    type: integer
                  workflowRefreshInterval:
                    default: 5m
                    type: string
                type: object
              queueRule:
                properties:
                  cooldown:
                    default: 2m
                    type: string
                  pollInterval:
                    default: 30s
                    type: string
                required:
                - cooldown
                - pollInterval
                type: object
              schedule:
                items:
                  properties:
                    base:
                      format: int32
                      minimum: 0
                      type: integer
                    days:
                      items:
                        maxLength: 16
                        type: string
                      maxItems: 7
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    from:
                      maxLength: 5
                      pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    to:
                      maxLength: 5
                      pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    tz:
                      maxLength: 64
                      pattern: ^[A-Za-z]+(?:/[A-Za-z0-9_+\-]+){0,2}$
                      type: string
                  required:
                  - base
                  - days
                  - from
                  - to
                  - tz
                  type: object
                  x-kubernetes-validations:
                  - message: from must be earlier than to
                    rule: self.from < self.to
                maxItems: 64
                type: array
              target:
                description: Exactly one of Arc or Garm MUST be set. Validated by
                  the controller and at admission.
                properties:
                  arc:
                    properties:
                      runnerSet:
                        properties:
                          name:
                            maxLength: 253
                            type: string
                          namespace:
                            maxLength: 253
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    required:
                    - runnerSet
                    type: object
                  garm:
                    properties:
                      pool:
                        properties:
                          name:
                            maxLength: 253
                            type: string
                          namespace:
                            maxLength: 253
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                    required:
                    - pool
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of target.arc or target.garm must be set
                  rule: '(has(self.arc) ? 1 : 0) + (has(self.garm) ? 1 : 0) == 1'
            required:
            - floor
            - github
            - queueRule
            - target
            type: object
          status:
            properties:
              activityFloor:
                format: int32
                type: integer
              activityLabelSets:
                items:
                  description: PredictedLabelSet is one entry in Status.PredictedLabelSets.
                  properties:
                    count:
                      format: int32
                      type: integer
                    labels:
                      items:
                        type: string
                      type: array
                  required:
                  - count
                  - labels
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              appliedFloor:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredFloor:
                format: int32
                type: integer
              lastDecreaseTime:
                description: |-
                  LastDecreaseTime is when the warm-floor was last decreased. It feeds the
                  scheduler's cooldown so decreases are rate-limited independently of the
                  per-poll reconcile time.
                format: date-time
                type: string
              lastQueueDepth:
                format: int32
                type: integer
              lastReconcileTime:
                format: date-time
                type: string
              predictedFloor:
                format: int32
                type: integer
              predictedLabelSets:
                items:
                  description: PredictedLabelSet is one entry in Status.PredictedLabelSets.
                  properties:
                    count:
                      format: int32
                      type: integer
                    labels:
                      items:
                        type: string
                      type: array
                  required:
                  - count
                  - labels
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
