name: two_stage_large
on:
  push:
jobs:
  stage1:
    runs-on: ubuntu-latest
    steps:
      - run: echo stage1
  stage2:
    needs: stage1
    runs-on: [self-hosted, large]
    steps:
      - run: echo stage2
